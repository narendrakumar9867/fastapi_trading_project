# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class StockDataActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.StockData]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await StockData.prisma().query_raw(
            'SELECT * FROM StockData WHERE datetime = $1',
            datetime.datetime.utcnow(),
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.StockData
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await StockData.prisma().query_first(
            'SELECT * FROM StockData WHERE open = $1',
            Decimal('1103527590.37740'),
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.StockDataCreateInput,
        include: Optional[types.StockDataInclude] = None
    ) -> _PrismaModelT:
        """Create a new StockData record.

        Parameters
        ----------
        data
            StockData record data
        include
            Specifies which relations should be loaded on the returned StockData model

        Returns
        -------
        prisma.models.StockData
            The created StockData record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a StockData record from just the required fields
        stockdata = await StockData.prisma().create(
            data={
                # data to create a StockData record
                'datetime': datetime.datetime.utcnow(),
                'open': Decimal('662824084.114790'),
                'high': Decimal('2035015474.36880'),
                'low': Decimal('1508029952.48625'),
                'close': Decimal('1062517886.26783'),
                'volume': 180171308,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.StockDataCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple StockData records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of StockData record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await StockData.prisma().create_many(
            data=[
                {
                    # data to create a StockData record
                    'datetime': datetime.datetime.utcnow(),
                    'open': Decimal('836760821.59533'),
                    'high': Decimal('790425851.211191'),
                    'low': Decimal('1149758321.164428'),
                    'close': Decimal('1388290519.164741'),
                    'volume': 1675546029,
                },
                {
                    # data to create a StockData record
                    'datetime': datetime.datetime.utcnow(),
                    'open': Decimal('1767274722.32627'),
                    'high': Decimal('1343201072.67578'),
                    'low': Decimal('744964398.196968'),
                    'close': Decimal('1116175964.86147'),
                    'volume': 1303003706,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.StockDataWhereUniqueInput,
        include: Optional[types.StockDataInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single StockData record.

        Parameters
        ----------
        where
            StockData filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned StockData model

        Returns
        -------
        prisma.models.StockData
            The deleted StockData record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        stockdata = await StockData.prisma().delete(
            where={
                'datetime': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.StockDataWhereUniqueInput,
        include: Optional[types.StockDataInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique StockData record.

        Parameters
        ----------
        where
            StockData filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StockData model

        Returns
        -------
        prisma.models.StockData
            The found StockData record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        stockdata = await StockData.prisma().find_unique(
            where={
                'datetime': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.StockDataWhereUniqueInput,
        include: Optional[types.StockDataInclude] = None
    ) -> _PrismaModelT:
        """Find a unique StockData record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            StockData filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StockData model

        Returns
        -------
        prisma.models.StockData
            The found StockData record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        stockdata = await StockData.prisma().find_unique_or_raise(
            where={
                'datetime': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StockDataWhereInput] = None,
        cursor: Optional[types.StockDataWhereUniqueInput] = None,
        include: Optional[types.StockDataInclude] = None,
        order: Optional[Union[types.StockDataOrderByInput, List[types.StockDataOrderByInput]]] = None,
        distinct: Optional[List[types.StockDataScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple StockData records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of StockData records returned
        skip
            Ignore the first N results
        where
            StockData filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StockData model
        order
            Order the returned StockData records by any field
        distinct
            Filter StockData records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.StockData]
            The list of all StockData records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 StockData records
        stockdatas = await StockData.prisma().find_many(take=10)

        # find the first 5 StockData records ordered by the high field
        stockdatas = await StockData.prisma().find_many(
            take=5,
            order={
                'high': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StockDataWhereInput] = None,
        cursor: Optional[types.StockDataWhereUniqueInput] = None,
        include: Optional[types.StockDataInclude] = None,
        order: Optional[Union[types.StockDataOrderByInput, List[types.StockDataOrderByInput]]] = None,
        distinct: Optional[List[types.StockDataScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single StockData record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StockData filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StockData model
        order
            Order the returned StockData records by any field
        distinct
            Filter StockData records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StockData
            The first StockData record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StockData record ordered by the low field
        stockdata = await StockData.prisma().find_first(
            skip=1,
            order={
                'low': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StockDataWhereInput] = None,
        cursor: Optional[types.StockDataWhereUniqueInput] = None,
        include: Optional[types.StockDataInclude] = None,
        order: Optional[Union[types.StockDataOrderByInput, List[types.StockDataOrderByInput]]] = None,
        distinct: Optional[List[types.StockDataScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single StockData record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StockData filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StockData model
        order
            Order the returned StockData records by any field
        distinct
            Filter StockData records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StockData
            The first StockData record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StockData record ordered by the close field
        stockdata = await StockData.prisma().find_first_or_raise(
            skip=1,
            order={
                'close': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.StockDataUpdateInput,
        where: types.StockDataWhereUniqueInput,
        include: Optional[types.StockDataInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single StockData record.

        Parameters
        ----------
        data
            StockData record data specifying what to update
        where
            StockData filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned StockData model

        Returns
        -------
        prisma.models.StockData
            The updated StockData record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        stockdata = await StockData.prisma().update(
            where={
                'datetime': datetime.datetime.utcnow(),
            },
            data={
                # data to update the StockData record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.StockDataWhereUniqueInput,
        data: types.StockDataUpsertInput,
        include: Optional[types.StockDataInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            StockData filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned StockData model

        Returns
        -------
        prisma.models.StockData
            The created or updated StockData record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        stockdata = await StockData.prisma().upsert(
            where={
                'datetime': datetime.datetime.utcnow(),
            },
            data={
                'create': {
                    'datetime': datetime.datetime.utcnow(),
                    'open': Decimal('1767274722.32627'),
                    'high': Decimal('1343201072.67578'),
                    'low': Decimal('744964398.196968'),
                    'close': Decimal('1116175964.86147'),
                    'volume': 1303003706,
                },
                'update': {
                    'open': Decimal('1767274722.32627'),
                    'high': Decimal('1343201072.67578'),
                    'low': Decimal('744964398.196968'),
                    'close': Decimal('1116175964.86147'),
                    'volume': 1303003706,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.StockDataUpdateManyMutationInput,
        where: types.StockDataWhereInput,
    ) -> int:
        """Update multiple StockData records

        Parameters
        ----------
        data
            StockData data to update the selected StockData records to
        where
            Filter to select the StockData records to update

        Returns
        -------
        int
            The total number of StockData records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all StockData records
        total = await StockData.prisma().update_many(
            data={
                'volume': 1686638315
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StockDataWhereInput] = None,
        cursor: Optional[types.StockDataWhereUniqueInput] = None,
    ) -> int:
        """Count the number of StockData records present in the database

        Parameters
        ----------
        select
            Select the StockData fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StockData filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StockDataCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await StockData.prisma().count()

        # results: prisma.types.StockDataCountAggregateOutput
        results = await StockData.prisma().count(
            select={
                '_all': True,
                'datetime': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.StockDataCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StockDataWhereInput] = None,
        cursor: Optional[types.StockDataWhereUniqueInput] = None,
    ) -> types.StockDataCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.StockDataCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StockDataWhereInput] = None,
        cursor: Optional[types.StockDataWhereUniqueInput] = None,
    ) -> Union[int, types.StockDataCountAggregateOutput]:
        """Count the number of StockData records present in the database

        Parameters
        ----------
        select
            Select the StockData fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StockData filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StockDataCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await StockData.prisma().count()

        # results: prisma.types.StockDataCountAggregateOutput
        results = await StockData.prisma().count(
            select={
                '_all': True,
                'open': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.StockDataCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.StockDataWhereInput] = None
    ) -> int:
        """Delete multiple StockData records.

        Parameters
        ----------
        where
            Optional StockData filter to find the records to be deleted

        Returns
        -------
        int
            The total number of StockData records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all StockData records
        total = await StockData.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.StockDataScalarFieldKeys'],
        *,
        where: Optional['types.StockDataWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.StockDataAvgAggregateInput'] = None,
        sum: Optional['types.StockDataSumAggregateInput'] = None,
        min: Optional['types.StockDataMinAggregateInput'] = None,
        max: Optional['types.StockDataMaxAggregateInput'] = None,
        having: Optional['types.StockDataScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.StockDataCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.StockDataScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.StockDataScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.StockDataGroupByOutput']:
        """Group StockData records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar StockData fields to group records by
        where
            StockData filter to select records
        take
            Limit the maximum number of StockData records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.StockDataGroupByOutput]
            A list of dictionaries representing the StockData record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group StockData records by high values
        # and count how many records are in each group
        results = await StockData.prisma().group_by(
            ['high'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models